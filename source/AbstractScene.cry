class AbstractScene {
	field _next = this;
	constructor() { }
	
	function switch_scene(scene) {
		this._next = scene;
	}
	
	function update(events) {
		Core.assert(false, "Override me!");
	}
	
	function render(rc) {
		Core.assert(false, "Override me!");
	}
}